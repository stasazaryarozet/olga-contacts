# Проект "Контакты": Автономная система построения социального графа

**Статус:** ✅ ПЛАТФОРМА (Platform Complete)  
**Дата:** 2025-11-01  
**Результат:** 464 entities (канонические), 5,050 edges, $0 budget  
**Версия:** v2.0 (Enhanced Platform with Entity Resolution)

---

## БЫСТРЫЙ СТАРТ

### Запуск системы (Практика)
📘 **[COMPLETE_GUIDE.md](./COMPLETE_GUIDE.md)** — Полное пошаговое руководство для запуска

### Техническая документация
- [DEPLOYMENT_README.md](./DEPLOYMENT_README.md) — Техническая документация
- [docs/EMAIL_PIPELINE.md](./docs/EMAIL_PIPELINE.md) — Email Pipeline (приоритет №1)
- [docs/EXPORT_CONTACTS.md](./docs/EXPORT_CONTACTS.md) — Импорт контактов
- [docs/SNOWBALLING.md](./docs/SNOWBALLING.md) — Автоматическое расширение графа

### Архитектурное проектирование (История)
- [AI_PROMPT_CONTACT_BUILDER.md](./AI_PROMPT_CONTACT_BUILDER.md) — Исходный запрос к ИИ
- [ARCHITECTURE_RESPONSE.md](./ARCHITECTURE_RESPONSE.md) — Архитектурное решение от Gemini
- [FINAL_ARCHITECTURE_GROQ.md](./FINAL_ARCHITECTURE_GROQ.md) — Финальная архитектура (Budget = $0)

---

## КРАТКОЕ ОПИСАНИЕ

Это архитектура и техническое задание для автономной системы, которая:
1. Принимает список публичных источников (статьи, LinkedIn, сайты)
2. Извлекает структурированный граф деловых связей
3. Работает без участия человека после инициализации

**Ключевая философия:** Human Effort → 0

---

## РЕАЛИЗОВАННАЯ АРХИТЕКТУРА (v2.0)

```
[Any Source] → add_fact() → Enhanced Graph DB (Entity Resolution)
                                ↓
                      ┌──────────┴──────────┐
                      ↓                     ↓
              Graph Zone              Context Zone
           (fast queries)          (provenance)
                      ↓
                Export:
           GraphML | JSON | SQL
                      ↓
         Gephi | D3.js | Neo4j | Streamlit
```

**Реализованный Stack:**
- **LLM:** Groq API (Llama 3.3 70B) — Free Tier
- **Storage:** SQLite (локально, файловая БД)
- **Entity Resolution:** Автоматическая дедупликация (1811 → 464 entities, x3.9)
- **Export:** GraphML (Gephi), JSON (D3.js)
- **Cost:** **$0/month** ✅

**Ключевые возможности:**
1. **Canonical IDs** — автоматическое слияние дубликатов
2. **Graph Zone / Context Zone** — разделение для производительности
3. **Universal Gateway** — `add_fact()` для любого источника
4. **Export** — GraphML, JSON для визуализации
5. **10 Target Scenarios** — готовые запросы к графу

---

## НОВОЕ В v2.0 (Platform Enhancement)

### 5 шагов от "Проекта" к "Платформе" (Gemini recommendation)

1. **✅ Канонизация (Canonical ID + Entity Resolution)**
   - Автоматическое слияние `john.doe@gmail.com` + `john.doe@work.com` → одна entity
   - Результат: 1811 → 464 узла (дедупликация x3.9)

2. **✅ Разделение (Graph Zone vs Context Zone)**
   - Graph Zone: быстрые запросы (indexed)
   - Context Zone: провenance, raw data

3. **✅ Портативность (GraphML / JSON Export)**
   - `export_to_graphml()` → Gephi, Neo4j Bloom
   - `export_to_json()` → D3.js, web UI

4. **✅ Шлюз (Единая функция `add_fact`)**
   - Универсальный интерфейс для всех источников
   - Автоматическая Entity Resolution при добавлении

5. **✅ Сценарии (10 целевых вопросов)**
   - Q1: С кем я встречался чаще всего в [год]?
   - Q2: Какие контакты "остыли"?
   - Q5: Кто самый "связанный" контакт?
   - Q7: Какие организации в графе?
   - ...и 6 других

**Документация:**
- 📘 [PLATFORM_ENHANCEMENT.md](./PLATFORM_ENHANCEMENT.md) — Подробное описание 5 шагов
- 📘 [FINAL_INSIGHTS.md](./FINAL_INSIGHTS.md) — Паттерны и lessons learned
- 📘 [REPORT_PLATFORM_COMPLETE.md](./REPORT_PLATFORM_COMPLETE.md) — Отчёт для Gemini

**Скрипты:**
- `scripts/migrate_to_enhanced.py` — Миграция из старой БД
- `scripts/target_scenarios.py` — 10 целевых сценариев
- `scripts/test_export.py` — Тестирование экспорта

---

## LEGACY АРХИТЕКТУРА (v1.0)

```
Gmail (IMAP) → Email Pipeline → Groq (Llama 3.3 70B) → SQLite Graph
Google Contacts (CSV) → Import Script → SQLite Graph
Graph Entities → DuckDuckGo Search → Groq IE → SQLite Graph (Snowballing)
```

**3 Pipeline (v1.0):**
1. **Email Pipeline** — извлечение контактов из Gmail (IMAP → Groq)
2. **Contacts Import** — импорт структурированных контактов (CSV/VCF → SQLite)
3. **Snowballing** — автоматическое расширение через веб-поиск (DuckDuckGo → Groq)

---

## ДОКУМЕНТАЦИЯ ПРОЕКТА

### Генезис и проектирование
1. **[AI_PROMPT_CONTACT_BUILDER.md](./AI_PROMPT_CONTACT_BUILDER.md)** — Запрос к Gemini (максимальный мета-уровень)
2. **[ARCHITECTURE_RESPONSE.md](./ARCHITECTURE_RESPONSE.md)** — Event-Driven Probabilistic Graph Pipeline
3. **[QUESTIONS_TO_GEMINI.md](./QUESTIONS_TO_GEMINI.md)** — 7 критических вопросов (русский язык, trade-offs, seed data)
4. **[GEMINI_RESPONSES.md](./GEMINI_RESPONSES.md)** — Детальные ответы на технические вопросы
5. **[FOLLOWUP_QUESTIONS_TO_GEMINI.md](./FOLLOWUP_QUESTIONS_TO_GEMINI.md)** — 8 вопросов уровня реализации
6. **[FINAL_PRACTICALITY_QUESTIONS.md](./FINAL_PRACTICALITY_QUESTIONS.md)** — 8 вопросов о практичности MVP
7. **[GEMINI_FINAL_ANSWERS.md](./GEMINI_FINAL_ANSWERS.md)** — Финальные решения по MVP scope

### Реализация
8. **[TASK_SOW.md](./TASK_SOW.md)** ⭐ — **ГЛАВНЫЙ ДОКУМЕНТ**: Полное техническое задание для инженера

### Историческое (deprecated)
9. **[ANSWERS_TO_CRITICAL_QUESTIONS.md](./ANSWERS_TO_CRITICAL_QUESTIONS.md)** — Ранние предложения (до Gemini ответов)

---

## ~~СТАРАЯ АРХИТЕКТУРА (Gemini, deprecated)~~

Ниже — первоначальная архитектура от Gemini (октябрь 2025). 
**НЕ ИСПОЛЬЗОВАТЬ** для реализации. См. [TASK_SOW.md](./TASK_SOW.md).

<details>
<summary>Развернуть историческую документацию</summary>

---

## 1. Стек технологий

Для создания автономной системы сбора данных (ETL + NLP pipeline) рекомендуется использовать Python как основной язык, благодаря его экосистеме для data science и веб-скрапинга.

| Компонент | Технология | Обоснование |
| :--- | :--- | :--- |
| **Оркестрация** | Apache Airflow | Управление пайплайном по расписанию (DAGs). Позволяет настроить ежедневный/еженедельный запуск, мониторинг и перезапуск упавших задач. |
| **Язык** | Python 3.10+ | Стандарт индустрии для NLP, скрапинга и обработки данных. |
| **Сбор (Crawling)** | Google Custom Search JSON API | Для первичного поиска новых URL по ключевым словам. Контролируемый и API-ориентированный способ, надежнее, чем "слепой" краулинг. |
| **Сбор (Scraping)** | Scrapy / BeautifulSoup | Scrapy для асинхронного и масштабного скрапинга известных сайтов (партнёры, СМИ). BeautifulSoup + requests для простого извлечения текста с URL, найденных через API. |
| **Извлечение (NER)** | Hugging Face Transformers (BERT) | Модели (например, `DeepPavlov/rubert-base-cased-ner`) на базе BERT для русского языка показывают SOTA-результаты для PER (Личности) и ORG (Организации). |
| **Извлечение (Relation)**| spaCy (Dependency Parser) | После NER, spaCy используется для синтаксического анализа предложений, чтобы понять связь между Ольгой Розет и найденной сущностью (e.g., "Ольга (nsubj) -> куратор (appos) -> Наталья (obj)"). |
| **Обогащение** | Regex (Python `re`) | Для поиска паттернов email (`name@domain.com`) и сайтов на целевых страницах, найденных на шаге обогащения. |
| **База данных** | Neo4j (Graph Database) | Идеально для этой задачи. Данные — это граф: (Ольга Розет) -[СО-КУРАТОР]-> (Наталья Логинова). Позволяет хранить сущности (PER, ORG) как узлы, а их связи как ребра. |
| **Хранилище текстов** | Elasticsearch / S3 | Elasticsearch для индексации и поиска по сырым текстам (статьям). S3 (или MinIO) для дешевого хранения HTML-дампов. |

## 2. Пошаговый Pipeline

Этот пайплайн должен быть реализован как DAG (Directed Acyclic Graph) в Apache Airflow, запускаемый, например, раз в 24 часа.

### Шаг 1: Сбор данных (Crawling)

1.  **Поиск источников (Seeding):**
    *   Задача Airflow обращается к Google Custom Search API.
    *   Выполняется N запросов, используя ключевые идентификаторы: "Ольга Розет дизайнер", "Творческая мастерская Ольги Розет", "Ольга Розет" AND "Наталья Логинова" и т.д.
    *   API возвращает список релевантных URL.
2.  **Фильтрация и дедупликация URL:**
    *   Новые URL сравниваются с уже обработанными в БД (Neo4j или Elasticsearch).
    *   Отсеиваются "шумные" домены (соцсети, агрегаторы), если они не являются целевыми.
3.  **Скрапинг (Scraping):**
    *   Одобренные URL добавляются в очередь для Scrapy.
    *   Scrapy-паук загружает страницу, извлекает основной текстовый контент (отбрасывая меню, футеры, рекламу) и сохраняет сырой текст в Elasticsearch/S3 с метаданными (URL, дата сбора).

### Шаг 2: Извлечение (Extraction)

1.  **NER (Named Entity Recognition):**
    *   Новые тексты из Elasticsearch обрабатываются моделью BERT (`rubert-base-cased-ner`).
    *   Модель извлекает все сущности `PER` (Имя) и `ORG` (Организация).
2.  **Извлечение связей (Relation Extraction):**
    *   Для каждого предложения, содержащего "Ольга Розет" (или ее псевдоним) И другую сущность `PER`/`ORG`, запускается spaCy.
    *   **Цель:** Мы не просто ищем имена, мы ищем контекст.
    *   Используя dependency parser (синтаксический парсер) spaCy, мы ищем паттерны:
        *   `(Ольга Розет) [conj] (Имярек) -> (Ольга и Имярек) -> Связь: "Партнёр/Коллега"`
        *   `(Имярек) [appos] (куратор) -> (Имярек, куратор)`
        *   `(Ольга Розет) [nsubj] [работает] [prep] (ВБШД) -> Связь: "Работает в"`
    *   **Результат:** Список кортежей: `(Entity_A, Entity_B, Relation_Type, Context_Snippet)`.

### Шаг 3: Обогащение (Enrichment)

1.  **Постановка задачи:** Для каждой извлеченной и валидированной сущности (e.g., `PER`: "Наталья Логинова", `ORG`: "Дизайн-бюро X"), необходимо найти контактные данные.
2.  **Целевой поиск:**
    *   Система снова использует Google Search API, но с более узкими запросами: `"Наталья Логинова" "дизайнер" "email"` или `"Дизайн-бюро X" "официальный сайт"`.
3.  **Микро-скрапинг и Regex:**
    *   Система скачивает топ-3 URL из этого поиска.
    *   Применяет набор Regex-правил для извлечения email, phone (не рекомендуется из-за PII) и URL (ссылки на сайт).
    *   Приоритет отдается "контактным" страницам (`/contacts`, `/about`).

### Шаг 4: Структурирование (Structuring)

1.  **Схема данных (Neo4j):**
    *   **Узлы (Nodes):**
        *   `(:Person {name: "Ольга Розет", type: "Target"})` - Целевой узел.
        *   `(:Person {name: "Наталья Логинова", canonical_name: "логинова наталья"})`
        *   `(:Organization {name: "ВБШД", canonical_name: "вбшд"})`
        *   `(:ContactDetail {type: "email", value: "contact@xyz.com"})`
        *   `(:Source {url: "http://smi.ru/article", crawled_at: "..."})`
    *   **Ребра (Edges):**
        *   `(:Person {name:"Ольга Розет"}) -[:СО-КУРАТОР {context: "snippet..."}]-> (:Person {name:"Наталья Логинова"})`
        *   `(:Person {name:"Ольга Розет"}) -[:ПРЕПОДАВАТЕЛЬ]-> (:Organization {name:"ВБШД"})`
        *   `(:Person {name:"Наталья Логинова"}) -[:HAS_CONTACT]-> (:ContactDetail {value: "..."})`
        *   `(:Person {name:"Наталья Логинова"}) -[:MENTIONED_IN]-> (:Source {url: "..."})`
2.  **Дедупликация (Entity Resolution):**
    *   **Проблема:** "Н. Логинова", "Наталья Логинова", "Loginova Natalya".
    *   **Решение:**
        *   **Канонизация:** Перед вставкой все имена приводятся к нижнему регистру, транслитерируются (если нужно), отбрасываются инициалы. `canonical_name` (см. схему).
        *   **Проверка:** Перед созданием нового узла `(:Person)` система ищет узел с таким же `canonical_name`.
        *   **Слияние (Merging):** Если `canonical_name` совпадает, система не создает новый узел, а добавляет новое ребро `[:MENTIONED_IN]` или `[:HAS_CONTACT]` к существующему узлу.
        *   Для сложных случаев (e.g., "Ольга" и "Ольга Розет") используются пороговые значения схожести строк (Levenshtein), но автоматика здесь рискованна.

## 3. Оценка рисков

### Технические риски

*   **Точность NER (False Positives):**
    *   **Риск:** Система извлекает "Ольга" (просто имя) или "Дизайн" (просто слово) как `PER` или `ORG`.
    *   **Минимизация:** Использование SOTA BERT-моделей (они контекстно-зависимы) и отбрасывание сущностей, которые не связаны синтаксически с "Ольгой Розет" (см. Шаг 2.2).
*   **Анти-скрапинг:**
    *   **Риск:** Сайты блокируют IP-адрес краулера (Cloudflare, CAPTCHA).
    *   **Минимизация:** Использование пула ротируемых прокси-серверов, user-agent ротация, соблюдение `robots.txt` и настройка `DOWNLOAD_DELAY` в Scrapy.
*   **Информационный "дрейф":**
    *   **Риск:** Алгоритм начинает находить контакты контактов, уходя далеко от Ольги Розет.
    *   **Минимизация:** Жесткое правило: в граф добавляются только сущности, имеющие прямую связь (ребро 1-го порядка) с узлом "Ольга Розет".

### Этические и юридические риски

*   **Обработка Персональных Данных (ФЗ-152 / GDPR):**
    *   **Риск:** Система автоматизированно собирает, хранит и структурирует ПДн (Имя, email) без явного согласия субъектов.
    *   **Минимизация (Юридическая):** Это основной риск. Единственное основание — обработка данных, "сделанных общедоступными субъектом ПДн". Однако сбор их в базу данных — это новая обработка.
    *   **Минимизация (Техническая):**
        *   **Безопасность:** Шифрование БД (at-rest, in-transit).
        *   **Минимизация данных:** Не хранить "чувствительные" данные, не хранить телефоны (высокий риск).
        *   **Прозрачность:** В системе должна быть реализована функция "полного удаления" (anonymization) любого контакта по запросу.
*   **Точность данных и репутационный риск:**
    *   **Риск:** Система неверно определяет связь. Например, статья "Ольга Розет раскритиковала Дизайнера X". Система сохраняет `(Ольга Розет) -[СВЯЗЬ]-> (Дизайнер X)`.
    *   **Минимизация:** Анализ тональности (Sentiment Analysis) на контекстном сниппете (`context: "snippet..."` в ребре Neo4j). Если тональность негативная, связь помечается флагом `FLAG_NEGATIVE` и не должна использоваться для "холодных" контактов.

</details>
