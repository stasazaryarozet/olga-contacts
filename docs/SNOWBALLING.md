# Snowballing: Автоматическое расширение графа

**Цель:** Автоматически найти публичные источники про контакты Ольги и расширить граф

---

## Как работает Snowballing?

```
Граф → Извлечь entities → Сгенерировать запросы → Поиск в web → Обработка URL → Новые entities → Граф
```

**Пример:**
1. В графе есть: `Ольга Розет → works_at → ВБШД`
2. Система генерирует запрос: `"Ольга Розет" "ВБШД"`
3. DuckDuckGo находит статьи/сайты
4. Система обрабатывает найденные URL через Groq
5. Извлекает новые связи, например: `ВБШД → located_in → Москва`

---

## Предварительные требования

- Граф уже должен содержать данные (из Email или Contacts)
- `GROQ_API_KEY` должен быть установлен

---

## Запуск

### Базовый запуск

```bash
cd contacts
source venv/bin/activate

export GROQ_API_KEY="твой_ключ"

python3 scripts/snowball.py
```

### С параметрами

```bash
python3 scripts/snowball.py \
  --anchor "Ольга Розет" \
  --max-entities 10 \
  --results-per-query 3 \
  --max-queries 5
```

---

## Параметры

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--anchor` | Имя человека для расширения | `Ольга Розет` |
| `--max-entities` | Сколько entities использовать для генерации запросов | `10` |
| `--results-per-query` | Сколько URL обработать на запрос | `3` |
| `--max-queries` | Максимум поисковых запросов | `5` |

---

## Что делает скрипт?

1. **Читает граф** — находит все связи anchor person (Ольга Розет)
2. **Генерирует запросы**:
   - Для организаций: `"Ольга Розет" "ВБШД"`
   - Для людей: `"Ольга Розет" "Наталья Логинова"`
   - Для событий: `"Ольга Розет" "Paris 2026"`
3. **Поиск в DuckDuckGo** (бесплатно, без API ключей)
4. **Обработка URL** через Groq IE pipeline
5. **Расширение графа** с новыми entities и relations

---

## Ограничения DuckDuckGo

- **Нет rate limits** (в отличие от Google API)
- **Качество результатов** немного ниже Google, но достаточно для MVP
- **Полностью бесплатно**, без регистрации

---

## Ожидаемое время

- **5 запросов × 3 URL** = 15 URL
- **~2 минуты на URL** (fetch + Groq)
- **Итого: ~30-40 минут**

---

## После выполнения

Проверить рост графа:

```bash
python3 -c "
import sys
sys.path.insert(0, 'src')
from graph_db import GraphDB

db = GraphDB()
stats = db.get_stats()

print('\n📊 GRAPH STATS:')
for k, v in stats.items():
    print(f'  {k}: {v}')

# Show Olga's expanded relations
rels = db.get_relations_for_person('Ольга Розет')
print(f'\n🔗 Ольга Розет: {len(rels)} relations')

if rels:
    print('\nNew relations:')
    for r in rels[-10:]:  # Last 10
        print(f\"  - {r['relation_type']}: {r['target_name']}\")

db.close()
"
```

---

## Когда запускать Snowballing?

**После того как граф заполнен базовыми данными:**

1. ✅ **Email Pipeline** выполнен (есть entities)
2. ✅ **Contacts Import** выполнен (есть organizations)
3. ➡️ **Snowballing** — расширить публичными источниками

---

## Альтернатива: Bing Search API

Если DuckDuckGo не работает или качество низкое, можно использовать **Bing Search API**:
- Free tier: 1000 запросов/месяц
- Регистрация: https://www.microsoft.com/en-us/bing/apis/bing-web-search-api

(Но это требует регистрации, поэтому DuckDuckGo предпочтительнее для MVP)

---

**Результат:** Граф расширяется автономно, находя связи из публичных источников

